import { StyleSheet } from 'react-native';

export const mainCircleStyles = StyleSheet.create({
  circle: {
    width: 100,
    height: 100,
    borderRadius: 50,
    backgroundColor: '#FFB6C1',
    justifyContent: 'center',
    alignItems: 'center',
    marginBottom: 20,
  },
  meowText: {
    fontSize: 20,
    color: 'white',
    fontWeight: 'bold',
  }
});
